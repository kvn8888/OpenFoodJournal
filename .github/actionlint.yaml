self-hosted-runner:
  # Keep the pinned hosted macOS label explicit for actionlint versions whose
  # runner catalog predates the current GitHub image.
  labels:
    - macos-26

config-variables:
  - ENABLE_APP_STORE_AUTOMATION
  - ENABLE_TESTFLIGHT_AUTOMATION
  - RELEASE_NOTES_MODEL
