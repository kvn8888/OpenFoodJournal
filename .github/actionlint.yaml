self-hosted-runner:
  # GitHub introduced this hosted preview label after actionlint 1.7.12's
  # built-in runner catalog was published.
  labels:
    - xcode-27

config-variables:
  - ENABLE_APP_STORE_AUTOMATION
  - ENABLE_TESTFLIGHT_AUTOMATION
